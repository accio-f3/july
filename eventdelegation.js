const shoppingList = document.getElementById('shopping-list');
shoppingList.addEventListener('click',(e)=>{

})

// 3 events to 1 event

// closures, promises,throttle , debouncing
// this keyword, object cloning, call , bind apply